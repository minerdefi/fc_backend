# Requirements optimized for cPanel shared hosting
# Some packages may need to be adjusted based on your hosting provider

# Core Django
Django==5.1
djangorestframework==3.15.2
djangorestframework_simplejwt==5.4.0
django-cors-headers==4.7.0

# Database
dj-database-url==2.1.0
# MySQL support (most cPanel hosts use MySQL)
mysqlclient==2.2.4
# Alternative MySQL driver if mysqlclient doesn't work
# PyMySQL==1.1.1

# PostgreSQL support (if your cPanel supports PostgreSQL)
# psycopg2-binary==2.9.9

# Static files and media
whitenoise==6.9.0
Pillow==10.4.0

# Environment and utilities
python-dotenv==1.0.1

# JWT and authentication
PyJWT==2.10.1

# Core Python packages
asgiref==3.8.1
packaging==24.2
sqlparse==0.5.1
typing_extensions==4.12.2
tzdata==2024.1

# WSGI server (may not be needed on shared hosting)
# gunicorn==23.0.0

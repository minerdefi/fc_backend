# Environment variables for Render deployment
# Copy these to your Render service environment variables

# Django settings
DEBUG=False
SECRET_KEY=your-secret-key-here-will-be-generated-by-render

# Database (will be automatically provided by Render PostgreSQL)
# DATABASE_URL=postgresql://user:password@host:port/database

# Email settings (optional - configure if you want email functionality)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password

# CORS settings (add your frontend URL)
FRONTEND_URL=https://wondrous-crumble-ab82ec.netlify.app
